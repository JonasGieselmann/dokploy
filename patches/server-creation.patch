diff --git a/apps/dokploy/server/api/routers/server.ts b/apps/dokploy/server/api/routers/server.ts
index 4a044ec5..fe81496d 100644
--- a/apps/dokploy/server/api/routers/server.ts
+++ b/apps/dokploy/server/api/routers/server.ts
@@ -46,12 +46,13 @@ export const serverRouter = createTRPCRouter({
 			try {
 				const user = await findUserById(ctx.user.ownerId);
 				const servers = await findServersByUserId(user.id);
-				if (IS_CLOUD && servers.length >= user.serversQuantity) {
-					throw new TRPCError({
-						code: "BAD_REQUEST",
-						message: "You cannot create more servers",
-					});
-				}
+				// PATCHED: Disabled server quantity check for self-hosted Cloud Run
+				// if (IS_CLOUD && servers.length >= user.serversQuantity) {
+				// 	throw new TRPCError({
+				// 		code: "BAD_REQUEST",
+				// 		message: "You cannot create more servers",
+				// 	});
+				// }
 				const project = await createServer(
 					input,
 					ctx.session.activeOrganizationId,
@@ -360,11 +361,11 @@ export const serverRouter = createTRPCRouter({
 				await removeDeploymentsByServerId(currentServer);
 				await deleteServer(input.serverId);
 
-				if (IS_CLOUD) {
-					const admin = await findUserById(ctx.user.ownerId);
-
-					await updateServersBasedOnQuantity(admin.id, admin.serversQuantity);
-				}
+				// PATCHED: Disabled server quantity tracking for self-hosted Cloud Run
+				// if (IS_CLOUD) {
+				// 	const admin = await findUserById(ctx.user.ownerId);
+				// 	await updateServersBasedOnQuantity(admin.id, admin.serversQuantity);
+				// }
 
 				return currentServer;
 			} catch (error) {
