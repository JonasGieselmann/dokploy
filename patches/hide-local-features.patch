diff --git a/apps/dokploy/components/layouts/side.tsx b/apps/dokploy/components/layouts/side.tsx
index 7473fe58..36b67587 100644
--- a/apps/dokploy/components/layouts/side.tsx
+++ b/apps/dokploy/components/layouts/side.tsx
@@ -258,14 +258,14 @@ const MENU: Menu = {
 	],
 
 	settings: [
-		{
-			isSingle: true,
-			title: "Web Server",
-			url: "/dashboard/settings/server",
-			icon: Activity,
-			// Only enabled for admins in non-cloud environments
-			isEnabled: ({ auth, isCloud }) => !!(auth?.role === "owner" && !isCloud),
-		},
+		// PATCHED: Disabled - Web Server settings need local Docker/Traefik
+		// {
+		// 	isSingle: true,
+		// 	title: "Web Server",
+		// 	url: "/dashboard/settings/server",
+		// 	icon: Activity,
+		// 	isEnabled: ({ auth, isCloud }) => !!(auth?.role === "owner" && !isCloud),
+		// },
 		{
 			isSingle: true,
 			title: "Profile",
@@ -338,14 +338,14 @@ const MENU: Menu = {
 			// Only enabled for admins
 			isEnabled: ({ auth }) => !!(auth?.role === "owner"),
 		},
-		{
-			isSingle: true,
-			title: "Cluster",
-			url: "/dashboard/settings/cluster",
-			icon: Boxes,
-			// Only enabled for admins in non-cloud environments
-			isEnabled: ({ auth, isCloud }) => !!(auth?.role === "owner" && !isCloud),
-		},
+		// PATCHED: Disabled - Cluster settings need local Docker Swarm
+		// {
+		// 	isSingle: true,
+		// 	title: "Cluster",
+		// 	url: "/dashboard/settings/cluster",
+		// 	icon: Boxes,
+		// 	isEnabled: ({ auth, isCloud }) => !!(auth?.role === "owner" && !isCloud),
+		// },
 		{
 			isSingle: true,
 			title: "Notifications",
