diff --git a/apps/dokploy/server/server.ts b/apps/dokploy/server/server.ts
index eaf56269..f160b6f7 100644
--- a/apps/dokploy/server/server.ts
+++ b/apps/dokploy/server/server.ts
@@ -60,6 +60,8 @@ void app.prepare().then(async () => {
 
 		if (IS_CLOUD && process.env.NODE_ENV === "production") {
 			await migration();
+			// PATCHED: Enable schedules for self-hosted Cloud Run (requires min 1 instance)
+			await initSchedules();
 		}
 
 		server.listen(PORT, HOST);
